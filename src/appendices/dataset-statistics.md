# Dataset Statistics

[Per-class counts for training and validation, with totals. Span-length
distribution in tokens, broken down by technique. OOV rate by
predicted class, computed via the `vocabulary` Bloom filter.]
